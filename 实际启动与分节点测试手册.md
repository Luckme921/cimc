# ROS 2 实际启动与分节点测试手册

## 0. 每个新终端的环境

```bash
source /opt/ros/humble/setup.bash
source ~/x86_ros2_ws/install/setup.bash
```

## 1. CAN 厂商库安装后检查

```bash
sudo ldconfig
ls -l /usr/local/include/controlcan.h /usr/local/lib/libcontrolcan.so
file /usr/local/lib/libcontrolcan.so
ldd /usr/local/lib/libcontrolcan.so | grep 'not found'
ldconfig -p | grep controlcan
```

`file` 应显示 `ELF 64-bit ... x86-64`；`ldd ... | grep not found` 应无输出。若 `ldconfig -p` 没有结果，检查 `/etc/ld.so.conf.d/libc.conf` 是否包含 `/usr/local/lib`，或临时：

```bash
export LD_LIBRARY_PATH=/usr/local/lib:${LD_LIBRARY_PATH:-}
```

## 2. 重新构建

```bash
cd ~/x86_ros2_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install \
  --packages-select cimc weld_controller chishine_camera_ros2 weld_seam_perception \
  --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

检查：

```bash
ros2 pkg executables cimc
ros2 pkg executables weld_controller
ldd "$(ros2 pkg prefix weld_controller)/lib/weld_controller/weld_controller_node" | grep 'not found'
```

## 3. 单节点测试

### 3.1 motor_control_node（需电机串口）

```bash
ls -l /dev/serial/by-id/
ros2 run cimc motor_control_node
```

另一终端：

```bash
ros2 topic pub --once /cimc/motor_speed std_msgs/msg/Float32 '{data: 3.0}'
ros2 topic pub --once /cimc/motor_speed std_msgs/msg/Float32 '{data: 0.0}'
```

### 3.2 data_receiver_node（可不接ABB启动）

本机网卡必须真正拥有 `192.168.125.2`：

```bash
ip -br addr
ros2 run cimc data_receiver_node
```

检查监听：

```bash
ss -lntp | grep 45000
ros2 topic echo /abb/raw_text
ros2 topic echo /abb/tx_status
```

没有ABB连接时模拟发送，应返回断线丢弃：

```bash
ros2 topic pub --once /abb/tx_text std_msgs/msg/String \
  "{data: 'TEST_TO_ABB\n'}"
```

### 3.3 weld_controller_node（需USB-CAN和焊机）

先只启动观察：

```bash
ros2 run weld_controller weld_controller_node
```

状态监听：

```bash
ros2 topic echo /weld/status
```

只有确认焊机、气路、送丝、硬件急停和CAN参数后才能执行：

```bash
ros2 topic pub --once /weld/control std_msgs/msg/String "{data: 'start_system'}"
ros2 topic pub --once /weld/set_param_real std_msgs/msg/Float32MultiArray \
  '{data: [250.0, 30.0]}'
```

不要在无安全准备时发 `start_welding`。

### 3.4 chishine_camera_node（需相机）

```bash
ros2 run chishine_camera_ros2 chishine_camera_node --ros-args \
  --params-file ~/x86_ros2_ws/src/chishine_camera_ros2/config/camera.yaml
```

另一终端：

```bash
ros2 service call /camera/capture std_srvs/srv/Trigger '{}'
ros2 topic echo /camera/pointcloud_file std_msgs/msg/String \
  --qos-durability transient_local
```

### 3.5 weld_seam_node（无相机也可测）

```bash
ros2 run weld_seam_perception weld_seam_node --ros-args \
  --params-file ~/x86_ros2_ws/src/weld_seam_perception/config/weld_seam.yaml
```

另一终端发已有PLY：

```bash
ros2 topic pub --once --qos-durability transient_local \
  /camera/pointcloud_file std_msgs/msg/String \
  "{data: '$SCUT_WELD_DATA_ROOT/pointclouds/20260529101148-1.ply'}"
```

监听：

```bash
ros2 topic echo /weld_seam/status
ros2 topic echo /weld_seam/result_csv std_msgs/msg/String \
  --qos-durability transient_local
```

### 3.6 weld_task_coordinator_node（需相机服务）

```bash
ros2 run cimc weld_task_coordinator_node
```

合法拍照指令格式（示例位姿，不是实机值）：

```text
START_CAPTURE:1200.0,100.0,800.0,1.0,0.0,0.0,0.0
```

参数顺序是 `Base_from_TCP` 的 `x_mm,y_mm,z_mm,qw,qx,qy,qz`。不带位姿的 `START_CAPTURE` 默认拒绝。

模拟ABB话题：

```bash
ros2 topic pub --once /abb/raw_text std_msgs/msg/String \
  "{data: 'START_CAPTURE:1200.0,100.0,800.0,1.0,0.0,0.0,0.0'}"
```

监听：

```bash
ros2 topic echo /weld_task/status
ros2 topic echo /weld_task/capture_tcp_pose_base geometry_msgs/msg/PoseStamped \
  --qos-durability transient_local
```

### 3.7 handeye_abb_bridge_node（首先禁止ABB发送）

方向未知时启动，会安全拒绝转换：

```bash
ros2 run cimc handeye_abb_bridge_node
```

假设矩阵为 `TCP_from_camera` 做离线计算：

```bash
ros2 run cimc handeye_abb_bridge_node --ros-args \
  -p matrix_direction:=tcp_from_camera \
  -p require_task_armed:=false \
  -p require_capture_pose:=false \
  -p send_to_abb:=false
```

然后启动 `weld_seam_node` 并发布已有PLY。监听：

```bash
ros2 topic echo /abb/trajectory_tcp geometry_msgs/msg/PoseArray \
  --qos-durability transient_local
ros2 topic echo /handeye_bridge/status
```

若要把一个模拟 `Base_from_TCP` 发给桥接节点：

```bash
ros2 topic pub --once --qos-durability transient_local \
  /weld_task/capture_tcp_pose_base geometry_msgs/msg/PoseStamped \
  "{header: {frame_id: robot_base}, pose: {position: {x: 1.2, y: 0.1, z: 0.8}, orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}}}"
```

## 4. 无相机完整模拟

无相机时不能调用真实 `/camera/capture`。可以不启动协调节点，手动模拟其三个输出：

1. 启动 `weld_seam_node`。
2. 启动桥接节点：

```bash
ros2 run cimc handeye_abb_bridge_node --ros-args \
  -p matrix_direction:=tcp_from_camera \
  -p send_to_abb:=false
```

3. 发布拍照TCP位姿：

```bash
ros2 topic pub --once --qos-durability transient_local \
  /weld_task/capture_tcp_pose_base geometry_msgs/msg/PoseStamped \
  "{header: {frame_id: robot_base}, pose: {position: {x: 1.2, y: 0.1, z: 0.8}, orientation: {x: 0.0, y: 0.0, z: 0.0, w: 1.0}}}"
```

4. 授权下一条轨迹：

```bash
ros2 topic pub --once --qos-durability transient_local \
  /weld_task/trajectory_armed std_msgs/msg/Bool '{data: true}'
```

5. 发布已有PLY：

```bash
ros2 topic pub --once --qos-durability transient_local \
  /camera/pointcloud_file std_msgs/msg/String \
  "{data: '$SCUT_WELD_DATA_ROOT/pointclouds/20260529101148-1.ply'}"
```

6. 检查 `/abb/trajectory_tcp`、`/handeye_bridge/status` 和结果PLY，禁止机器人运动。

## 5. 真实工作启动顺序

### 终端1：ABB TCP

```bash
ros2 run cimc data_receiver_node
```

### 终端2：相机

```bash
ros2 run chishine_camera_ros2 chishine_camera_node --ros-args \
  --params-file ~/x86_ros2_ws/src/chishine_camera_ros2/config/camera.yaml
```

### 终端3：焊缝算法

```bash
ros2 run weld_seam_perception weld_seam_node --ros-args \
  --params-file ~/x86_ros2_ws/src/weld_seam_perception/config/weld_seam.yaml
```

### 终端4：任务协调

```bash
ros2 run cimc weld_task_coordinator_node
```

### 终端5：手眼桥接（第一阶段不发ABB）

```bash
ros2 run cimc handeye_abb_bridge_node --ros-args \
  -p matrix_direction:=tcp_from_camera \
  -p send_to_abb:=false
```

### 终端6：状态监控

```bash
ros2 topic echo /weld_task/status
```

另开终端监控：

```bash
ros2 topic echo /handeye_bridge/status
ros2 topic echo /abb/tx_status
ros2 topic echo /weld_seam/status
```

当以下事项全部确认后，才改为 `send_to_abb:=true`：

1. `handEyeMatrix` 方向已用实际标定软件/文档确认；
2. ABB发来的确实是拍照瞬间 `Base_from_TCP`；
3. ABB和ROS四元数的顺序、左右手系、工具方向已确认；
4. 用已知标定块/实物点检查变换误差；
5. ABB RAPID 已按换行符解析轨迹并检查点数；
6. 机器人处于手动低速、不起弧、有硬件急停的调试状态。

## 6. 焊机和电机启动时机

`weld_logic_node` 目前还是固定点数工艺逻辑，与算法可变轨迹点数不完全匹配。因此在完成后续优化前：

- 可单独测试 `motor_control_node`；
- 可单独测试 `weld_controller_node`；
- 不要在真实自动轨迹中启动 `weld_logic_node`。
