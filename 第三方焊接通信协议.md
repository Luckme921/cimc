# 192.168.3.5 第三方焊接通信协议（JSONL v1）

## 1. 职责和连接方向

- 第三方设备：`192.168.3.5`，负责根据 ABB 点位/状态决定送气、起焊、电流、旋转速度和停焊时机。
- x86 工控机：负责校验协议字段，把请求直接发布到现有焊机和旋转电机 ROS 话题，并回传 ABB 数据、焊机真实反馈和协议 ACK。
- `192.168.3.5` 建立 TCP 服务端，默认监听 `50000`。
- x86 的 `data_receiver_node` 作为 TCP 客户端主动连接 `192.168.3.5:50000`。
- 同一 TCP 连接全双工通信，不再混发无边界的 ABB 裸文本与焊机裸二进制。

## 2. 公共帧格式

协议名为 JSONL v1。每帧必须是一个单行 UTF-8 JSON 对象，并以一个换行字节 `\n`（`0x0A`）结束；允许在换行前带 `\r`。单帧默认不超过 4096 字节。

每帧公共字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `version` | 整数 | 固定为 `1` |
| `type` | 字符串 | 消息类型 |
| `seq` | 非负整数 | 发送方序号；第三方请求在每次 TCP 连接内必须严格递增 |

TCP 可能拆包或粘包，双方都必须按 `\n` 累积和拆分完整帧，不能把一次 `recv()` 当成一条消息。

## 3. 192.168.3.5 发给 x86

### 3.1 高级动作命令

```json
{"version":1,"type":"command","seq":1,"command":"GAS_ON"}
{"version":1,"type":"command","seq":3,"command":"WELD_START"}
{"version":1,"type":"command","seq":6,"command":"WELD_STOP"}
{"version":1,"type":"command","seq":7,"command":"FAULT_RESET"}
```

实际 TCP 字节流中每个 JSON 对象末尾还必须追加 `\n`。

固定映射：

| 第三方命令 | x86 发布的现有 ROS 话题 | 含义 |
|---|---|---|
| `GAS_ON` | `/weld/control`: `use_builtin_curve`、`start_system`、`start_gas` | 重申一元模式，启动 CAN 通信并送气 |
| `WELD_START` | `/weld/control`: `start_welding` | 开始焊接 |
| `WELD_STOP` | `/weld/control`: `stop_welding`；`/cimc/motor_speed`: `0.0` | 停焊并停止旋转电机 |
| `FAULT_RESET` | `/weld/control`: `fault_reset` | 焊机故障复位脉冲 |

x86 不再根据 P1、P2 等点号选择工艺；点号和工艺时序由 192.168.3.5 管理。旧 `weld_logic_node` 不参与此链路。

### 3.2 实时设定值

```json
{"version":1,"type":"setpoints","seq":2,"current_a":250.0,"rotation_speed_rps":6.0}
{"version":1,"type":"setpoints","seq":4,"current_a":245.0,"rotation_speed_rps":5.5}
```

- `current_a`：一元模式焊接电流，默认允许 `1.0–350.0 A`。
- `rotation_speed_rps`：旋转速度，默认软件限制 `0.0–6.0 r/s`；当前上限取历史已使用值，只有核对最终电机规格后才允许提高。
- x86 将其直接转换为：
  - `/weld/set_param_real`：`[current_a, 20.0]`；第二项是兼容底层接口的占位电压，一元内置曲线下不参与压强计算。
  - `/cimc/motor_speed`：`rotation_speed_rps`。

焊接过程中可连续发送新的 `setpoints`，每帧使用新的递增 `seq`。

正常工艺顺序由第三方保证：`GAS_ON -> 初始 setpoints -> WELD_START -> 若干 setpoints -> WELD_STOP`。x86 只做协议和值域校验，不根据 P 点号推断工艺，也不会替第三方自动补发起焊或参数切换。

## 4. x86 发给 192.168.3.5

### 4.1 请求 ACK

```json
{"version":1,"type":"ack","seq":101,"request_seq":2,"accepted":true,"message":"published current=250.0 A and rotation_speed=6.0 r/s"}
```

- `seq` 是 x86 自己的出站序号。
- `request_seq` 对应第三方请求序号；无法解析请求时可能为 `null`。
- `accepted=true` 只表示协议校验通过且对应 ROS 消息已发布，不表示焊机已经起弧、电机已经达到转速或物理动作成功。
- 物理结果必须以 `weld_feedback` 和设备状态为准。

### 4.2 焊机真实反馈

`weld_controller_node` 每收到一个 TPDO1 六字节帧，x86 就发送一条：

```json
{"version":1,"type":"weld_feedback","seq":102,"weld_ready":true,"weld_fault":false,"locate_success":false,"arc_success":true,"arc_error":false,"gas_error":false,"wire_stick":false,"param_out_of_range":false,"current_a":248,"voltage_v":29.7,"raw_hex":"010100F80129"}
```

`raw_hex` 始终是原始六字节的大写十六进制，便于第三方复核；其解析规则为：

- 字节 0 bit0/bit1/bit2：就绪/综合故障/寻位成功；
- 字节 1 bit0/bit1/bit2/bit3/bit7：起弧成功/电弧异常/气体异常/粘丝/参数越界；
- 字节 2–3：大端无符号电流，单位 A；
- 字节 4–5：大端无符号电压原始值，乘 `0.1 V`。

### 4.3 ABB 原始接收数据

```json
{"version":1,"type":"abb_rx","seq":103,"payload_ascii":"P1:100,200,300,...\n","raw_hex":"50313A3130302C...0A"}
```

- `payload_ascii` 便于直接读取 P1 等文本。
- `raw_hex` 可无损恢复 ABB 发来的原始 TCP 数据块。
- `abb_rx` 表示一次 TCP 接收块，不承诺刚好是一条 ABB 业务消息；需要完整消息时仍应按 ABB 自身换行符拼接。

## 5. 错误、断线和安全行为

- 非法 JSON、错误版本、未知命令、字段缺失、非有限数值、越界值或不递增的请求序号都不会发布控制话题，并返回 `accepted=false` 的 ACK。
- 每次 TCP 重连后，第三方请求序号可重新从 `0` 或 `1` 开始。
- 断线期间积压的旧 ABB/焊机实时帧在重连时丢弃，重连后只发送新数据。
- x86 接受过 `WELD_START` 后如果第三方 TCP 断开，默认发布 `stop_welding` 和电机 `0.0 r/s`。该行为可通过 `stop_on_third_party_disconnect` 配置，但生产环境不建议关闭。
- 软件停焊不能代替焊机急停、机器人急停或硬接线安全回路。

## 6. 当前节点和配置

网络协议和直接 ROS 映射位于：

- `src/cimc/cimc/data_receiver_node.py`
- `src/cimc/config/data_receiver.yaml`

底层执行节点保持不变：

- `weld_controller_node`：订阅 `/weld/control` 与 `/weld/set_param_real`；
- `motor_control_node`：订阅 `/cimc/motor_speed`。

这三个节点没有加入 `camera_weld_handeye_test.launch.py`，必须在完成设备安全检查后单独启动。
